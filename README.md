# 🔗 URL Shortener SaaS

Повнофункціональний сервіс скорочення URL, написаний на **Rust** 🦀 з використанням Cloudflare Workers.

## ✨ Особливості

- ⚡ **Блискавична швидкість** - редіректи за <50мс завдяки Cloudflare Edge Network
- 📊 **Детальна аналітика** - відстежування кліків, географії, джерел трафіку
- 🔐 **API для розробників** - REST API з автентифікацією
- 💰 **Freemium модель** - безкоштовний тариф + платні плани
- 🌍 **100% безкоштовний хостинг** - GitHub Pages + Cloudflare Workers
- 🦀 **Написано на Rust** - максимальна продуктивність та безпека

## 🏗️ Архітектура

```
┌─────────────────┐
│  GitHub Pages   │  ← Фронтенд (HTML/CSS/JS)
│   (Frontend)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Cloudflare      │  ← Backend (Rust + WebAssembly)
│   Workers       │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌──────┐
│  KV   │ │  D1  │  ← Зберігання даних
│Storage│ │ DB   │
└───────┘ └──────┘
```

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
# Встановити Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Встановити wrangler CLI
npm install -g wrangler

# Встановити worker-build
cargo install worker-build
```

### 2. Налаштування Cloudflare

```bash
# Авторизуватися в Cloudflare
wrangler login

# Створити KV namespace
wrangler kv:namespace create "URLS"

# Створити D1 database
wrangler d1 create url_shortener_db

# Оновити wrangler.toml з отриманими ID
```

### 3. Ініціалізація бази даних

```bash
# Створити таблиці в D1
wrangler d1 execute url_shortener_db --file=./schema.sql
```

### 4. Локальна розробка

```bash
# Запустити локально
wrangler dev

# Тестувати API
curl -X POST http://localhost:8787/api/shorten \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com"}'
```

### 5. Деплой

```bash
# Деплой Worker на Cloudflare
wrangler deploy

# Деплой фронтенду на GitHub Pages
cd static
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/shortlink.git
git push -u origin main

# Увімкнути GitHub Pages в налаштуваннях репозиторію
```

## 📡 API Документація

### Скоротити URL

```bash
POST /api/shorten
Content-Type: application/json

{
  "url": "https://example.com/very/long/url",
  "custom_alias": "my-link"  // опціонально
}

Response:
{
  "short_url": "https://blatik.github.io/shortlink/abc123",
  "short_code": "abc123",
  "original_url": "https://example.com/very/long/url"
}
```

### Редірект

```bash
GET /:code

Response: 301 Redirect to original URL
```

### Список посилань

```bash
GET /api/urls
Authorization: Bearer YOUR_API_KEY

Response:
[
  {
    "id": "uuid",
    "short_code": "abc123",
    "original_url": "https://example.com",
    "clicks": 42,
    "created_at": "2024-01-01T00:00:00Z"
  }
]
```

### Видалити посилання

```bash
DELETE /api/urls/:id
Authorization: Bearer YOUR_API_KEY

Response:
{
  "success": true
}
```

## 💰 Монетизація

### Тарифні плани

| План | Ціна | Посилань/міс | Аналітика | API | Власні домени |
|------|------|--------------|-----------|-----|---------------|
| Free | $0 | 100 | 7 днів | ❌ | ❌ |
| Pro | $9 | 10,000 | 90 днів | ✅ | ✅ |
| Business | $29 | Необмежено | 365 днів | ✅ | ✅ |

### Інтеграція Stripe

1. Створити акаунт на [stripe.com](https://stripe.com)
2. Отримати API ключі
3. Додати секрети:

```bash
wrangler secret put STRIPE_SECRET_KEY
wrangler secret put STRIPE_WEBHOOK_SECRET
```

## 📊 Аналітика

Відстежуються наступні метрики:
- 📈 Загальна кількість кліків
- 🌍 Географія (країна)
- 🔗 Джерела трафіку (referer)
- 📱 User Agent (пристрої)
- 📅 Кліки по датах

## 🔒 Безпека

- ✅ CORS налаштовано
- ✅ Rate limiting по IP
- ✅ Валідація URL
- ✅ API key автентифікація
- ✅ Stripe webhook signature verification

## 📈 Масштабування

**Безкоштовні ліміти:**
- Cloudflare Workers: 100,000 запитів/день
- KV Storage: 1GB
- D1 Database: 5GB

**Коли потрібно платити:**
- Workers: $5/міс за 10M запитів
- KV: $0.50/GB
- D1: $5/міс за 25GB

## 🛠️ Розробка

### Структура проекту

```
url_shortener/
├── src/
│   ├── lib.rs           # Головний entry point
│   ├── models/          # Моделі даних
│   ├── handlers/        # API handlers
│   └── utils/           # Утиліти
├── docs/                # Frontend assets (GitHub Pages)
├── Cargo.toml           # Rust залежності
├── wrangler.toml        # Cloudflare конфігурація
├── schema.sql           # Database schema
└── README.md
```

### Тестування

```bash
# Запустити тести
cargo test

# Перевірити код
cargo clippy

# Форматування
cargo fmt
```

## 📝 Ліцензія

MIT

## 🤝 Контрибуція

Pull requests вітаються!

## 📧 Підтримка

Питання? Створіть issue на GitHub або напишіть на email.

---

Створено з 🦀 Rust + ❤️
